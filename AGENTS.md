# Workspace Rules & Preferences

## Git Push Policy
- **NO AUTO GIT PUSH**: Never automatically execute `git push` or `git push origin <branch>` after completing tasks or making edits.
- Only run `git push` when explicitly asked by the user in the prompt box (e.g., "push to git", "push code", "git push") or when run manually by the user in the terminal.
